# ROADMAP.md - Yarukoto 開発ロードマップ

v1.0.0リリース後の課題・機能追加を段階的に実装するための計画。
各課題の詳細は `ISSUES.md` を参照。

## 方針

- **開発フロー**: バージョンの全内容を実装 → 検証 → リリース → 次バージョンの全内容を実装 → …を繰り返す
- **リリース単位**: 1セクション分（v1.2.0なら2-1〜2-4すべて）の実装・検証が完了した時点でリリース
- **枝番単位**: 1回のプロンプト指示でまとめて実装する範囲
- **DB互換性**: 全バージョンでALTER TABLE ADD / CREATE TABLE / INSERTのみ使用し、既存データとの後方互換を維持
- **「検討中・保留中」の3件**（孫タスク、フィードバック、開発者支援）は本ロードマップに含まない

## 検証ステップ定義

> 各STEPの詳細なプロンプトは `dev-flow-guide.md` のフェーズ4を参照。

| STEP | 内容 |
|------|------|
| STEP A | 機能検証（正常系 + 異常系・境界値 + 状態遷移・データ件数を一括検証） |
| STEP B | 品質レビュー（エラーハンドリング + UI/文言の一貫性を一括レビュー） |
| STEP C | 設計レビュー（目的適合性 + 実用性・耐久性。メジャーバージョンアップ時のみ） |
| STEP R | リグレッションテスト（既存機能の破壊がないか、影響範囲を特定して再確認） |

---



## v1.3.1 — コード構造リファクタリング

> v1.3.0 で肥大化したファイル群（TaskList.js 1032行、settings/page.js 893行、today/page.js 859行）を分割・整理し、v1.4.0 以降の機能追加に備える。ユーザー向けの機能変更なし。詳細は `refactoring-plan.md` を参照。

### R-1. 共通ユーティリティ抽出（Phase 0） ✅ 完了
- **対象**: `lib/utils.js` 新設、`hooks/useFilterOptions.js` 新設、`lib/taskSorter.js` 新設
- **内容**: 全ファイルで重複している getDb ボイラープレート、formatMin、todayStr、parseTags、フィルタ options 生成、ソートロジックを共通化
- **影響範囲**: 全コンポーネントファイル（import パス変更）
- **検証**: STEP A + STEP R
- **作業量**: 普通

### R-2. TaskList.js 分割（Phase 1） ✅ 完了
- **対象**: `components/TaskItem.js` 抽出、`hooks/useTaskActions.js` 新設、`hooks/useTaskDnD.js` 新設、`components/DndGaps.js` 新設
- **内容**: TaskList.js（1032行）から TaskItem コンポーネント、DB操作ハンドラ、DnD ロジックを分離し約350行に削減
- **検証**: STEP A + STEP R
- **作業量**: 重い

### R-3. today/page.js スリム化（Phase 2） ✅ 完了
- **対象**: `hooks/useTodayTasks.js` 新設、`hooks/useDragReorder.js` 移動・汎用化、ステータス変更ハンドラ共通化
- **内容**: today/page.js（859行）からデータ取得・マージロジック、DnD ハンドラを分離し約350行に削減
- **検証**: STEP A + STEP R
- **作業量**: 普通

### R-4. settings タブ分割 + routines 整理（Phase 3 + 4） ✅ 完了
- **対象**: `app/settings/_components/` 配下に TagsPanel / StatusPanel / OptionsPanel / DataPanel 新設、`app/routines/_components/RoutineFormModal.js` 新設（任意）
- **内容**: settings/page.js（893行）を約80行のタブシェルに削減。routines/page.js は余裕があれば整理
- **検証**: STEP A + STEP B + STEP R（最終枝番）
- **作業量**: 普通

### リリース前検証
- STEP A + STEP R
- ※ ユーザー向け機能変更なしのため STEP B は省略（R-4 で実施済み）
- ※ ビルド確認（`npm run tauri build`）は実施すること

---

## v1.4.0 — 新画面・構造拡張

> ダッシュボード改善、新画面追加、プロジェクト管理、カスタムフィールドなど機能面の拡張。v1.3.0で整備した並び替え・アーカイブ機能が前提。

### 4-1. ダッシュボード改善 + 子タスク自動完了オプション
- **ENH-1**: ダッシュボードで今日完了したタスクを可視化（UI提案ベースで実装時に検討）
- **ENH-5**: 「子タスクが全完了したら親タスクも完了にする」オプション
- **DB変更**: `app_settings` に自動完了オプション INSERT
- **検証**: STEP A + STEP B + STEP R（DB変更あり）
- **作業量**: 普通

### 4-2. 「やったタスク」画面（新規）
- **ENH-4**: `completed_at` をもとに過去の完了タスクをカレンダービューで振り返る新画面
  - サイドバーナビに追加
- **検証**: STEP A + STEP B + STEP R（UI変更あり）
- **作業量**: 重い

### 4-3. プロジェクト機能
- **ENH-2**: タスク・ルーティンをプロジェクト単位でフォルダ管理
  - デフォルトは「General」プロジェクト
  - プロジェクトのアーカイブ・復元
  - 「やるタスク」画面でプロジェクト名・親タスク名を表示
- **DB変更**: `projects` テーブル新設、`tasks` / `routines` に `project_id` カラム追加（既存データはGeneralに自動割当）
- **検証**: STEP A + STEP B + STEP R（DB変更あり）
- **作業量**: 重い

### 4-4. ユーザー定義カスタムフィールド
- **ENH-3**: タスクの入力項目をユーザーが自由に追加・削除
  - テキスト・リスト・数字の3タイプ
- **DB変更**: `custom_fields` / `task_custom_values` テーブル新設
- **検証**: STEP A + STEP B + STEP R（バージョン最終枝番）
- **作業量**: 重い

### リリース前検証
- STEP A + STEP B + STEP R

---

## v2.0.0 — UI大改修

> AI-Made感からの脱却を目的とした、全画面にわたるデザインリニューアル。全機能が揃った状態で一括してUIを刷新する。

### 5-1. UI全面リニューアル + テーマシステム
- **IMP-8**: テーマカラー変更（ライト/ダーク + 配色パターン）を含む
- 全画面のデザイン刷新（レイアウト・配色・タイポグラフィ・コンポーネントデザイン）
- `globals.css` のCSS変数をテーマごとに切り替え
- `app_settings` にテーマ設定を保存
- **検証**: STEP A + STEP B + STEP C + STEP R（メジャーバージョン最終枝番）
- **作業量**: 重い
- **備考**: 具体的なデザイン方針・枝番構成は実装開始時に策定

### リリース前検証
- STEP A + STEP B + STEP C + STEP R

---

## 全体スケジュール概観

```
v1.0.0
    │
    │  ← v1.1.0 ✅ リリース済（2026-02-25）
    ├─ 1-1  BUG-1 + BUG-2 ✅
    ├─ 1-2  IMP-10 + IMP-11 ✅
    ├─ 1-3  IMP-9 + IMP-5 ✅
    └─ 1-4  IMP-1 + IMP-6 ✅
    │
    ▼ リリース済
v1.1.0
    │
    │  ← v1.2.0 ✅ リリース済（2026-02-28）
    ├─ 2-1  BUG-4 + BUG-6 ✅
    ├─ 2-2  BUG-5 + IMP-12 + ENH-6 ✅
    ├─ 2-3  IMP-3 ✅
    └─ 2-4  IMP-7 ✅
    │
    ▼ リリース済
v1.2.0
    │
    │  ← v1.3.0 ✅ リリース済（2026-03-01）
    ├─ 3-1  BUG-3 ✅
    ├─ 3-2  IMP-2 ✅
    └─ 3-3  IMP-4 ✅
    │
    ▼ リリース済
v1.3.0（現在地）
    │
    │  ← v1.3.1 リファクタリング（R-1〜R-4 すべて完了後にリリース）
    ├─ R-1  Phase 0: 共通ユーティリティ抽出 ✅ 完了
    ├─ R-2  Phase 1: TaskList.js 分割 ✅ 完了
    ├─ R-3  Phase 2: today/page.js スリム化 ✅ 完了
    └─ R-4  Phase 3+4: settings + routines 分割 ✅ 完了
    │
    ▼ 検証・リリース
v1.3.1
    │
    │  ← v1.4.0 実装内容（4-1〜4-4 すべて完了後に検証・リリース）
    ├─ 4-1  ENH-1 + ENH-5
    ├─ 4-2  ENH-4
    ├─ 4-3  ENH-2
    └─ 4-4  ENH-3
    │
    ▼ 検証・リリース
v1.4.0
    │
    │  ← v2.0.0 実装内容（UI大改修、枝番構成は実装開始時に策定）
    └─ 5-1  IMP-8 + UI全面リニューアル
    │
    ▼ 検証・リリース
v2.0.0
```

**合計**: リリース6回（v1.1.0済 + v1.2.0済 + v1.3.0済 + 残り3回）
