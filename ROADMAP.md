# ROADMAP.md - Yarukoto 開発ロードマップ

v1.0.0リリース後の課題・機能追加を段階的に実装するための計画。
各課題の詳細は `ISSUES.md` を参照。

## 方針

- **開発フロー**: バージョンの全内容を実装 → 検証 → リリース → 次バージョンの全内容を実装 → …を繰り返す
- **リリース単位**: 1セクション分（v1.2.0なら2-1〜2-4すべて）の実装・検証が完了した時点でリリース
- **枝番単位**: 1回のプロンプト指示でまとめて実装する範囲
- **DB互換性**: 全バージョンでALTER TABLE ADD / CREATE TABLE / INSERTのみ使用し、既存データとの後方互換を維持
- **「検討中・保留中」の3件**（孫タスク、フィードバック、開発者支援）は本ロードマップに含まない

## 検証ステップ定義

> 各STEPの詳細なプロンプトは `dev-flow-guide.md` のフェーズ4を参照。

| STEP | 内容 |
|------|------|
| STEP A | 機能検証（正常系 + 異常系・境界値 + 状態遷移・データ件数を一括検証） |
| STEP B | 品質レビュー（エラーハンドリング + UI/文言の一貫性を一括レビュー） |
| STEP C | 設計レビュー（目的適合性 + 実用性・耐久性。メジャーバージョンアップ時のみ） |
| STEP R | リグレッションテスト（既存機能の破壊がないか、影響範囲を特定して再確認） |

---

## ✅ v1.1.0 — バグ修正 + コア基盤整備【リリース済 2026-02-25】

> ステータスモデルの変更・表示改善・ユーティリティなど、後続機能の土台になるものを先に固める。

### 1-1. バグ修正 2件 ✅
- **BUG-1**: 「＋」ボタンからの子タスク追加時にタグが継承されない
- **BUG-2**: データ全削除時にルーティンが削除されない

### 1-2. キャンセルステータス + 完了日表示 ✅
- **IMP-10**: キャンセルステータスの追加（status_code=5、グレーアウト+取り消し線）
- **IMP-11**: 完了日（`completed_at`）の各画面への表示（読み取り専用）
- **DB変更**: `status_master` にキャンセル(code=5) INSERT

### 1-3. トースト通知 + タスク入力UI改善 ✅
- **IMP-9**: 操作時のトースト通知（追加・編集・削除・アーカイブ、FABの上に表示）
- **IMP-5**: タスク入力フォームの項目順整理・詳細設定のアコーディオン化

### 1-4. やるタスク画面の改善 + 設定オプション追加 ✅
- **IMP-1**: 「やるタスク」画面からのタスク編集（削除不可）
- **IMP-6**: 「期限切れタスクを今日やるタスクに表示する」オプション
- **DB変更**: `app_settings` に `show_overdue_in_today` INSERT

---

## v1.2.0 — 親子タスク修正 + フィルタ・UI改善

> 重要度「高」のバグ修正を最優先で対応し、フィルタ改善とUI統一でタスク操作の品質を向上させる。

### 2-1. 親子タスクのデータ整合性修正 ✅
- **BUG-4**: 親タスク削除時に子タスクも削除される → 削除前に子の `parent_id` を NULL 化して独立させる
- **BUG-6**: 子タスクを持つタスクに親タスクを設定できてしまう → 2階層制限バリデーション追加
- **検証**: STEP A + STEP B + STEP R（DB変更あり）
- **作業量**: 普通

### 2-2. タスクUI改善 3点 ✅
- **BUG-5**: タスクの編集と追加でレイアウトが異なる → 項目順の統一
- **IMP-12**: ステータスチェックボックスの着手中→未着手戻し対応
- **ENH-6**: 完了日（`completed_at`）の即時UI反映
- **検証**: STEP A + STEP B + STEP R（UI変更あり）
- **作業量**: 普通

### 2-3. フィルタ複数選択
- **IMP-3**: フィルタの複数選択（ステータスは「完了・キャンセルを除く」トグル、他はチップON/OFF方式）
- **検証**: STEP A + STEP B + STEP R（UI変更あり）
- **作業量**: 重い

### 2-4. タグアーカイブ
- **IMP-7**: 不要タグのアーカイブ（既存データに影響なく選択肢から非表示）
- **DB変更**: `tags` テーブルに `archived INTEGER DEFAULT 0` カラム追加
- **検証**: STEP A + STEP B + STEP R（バージョン最終枝番）
- **作業量**: 軽い

---

## v1.3.0 — アーカイブ・並び替え・設定管理

> タスク一覧の使い勝手に直結する中〜大規模機能群。v1.2.0で整備したフィルタ改善・UI統一が前提。

### 3-1. ステータス並び順変更
- **BUG-3**: ステータスの並び順変更ができない → 設定画面で `sort_order` をDnDまたは上下ボタンで変更可能に
- **検証**: STEP A + STEP B + STEP R（UI変更あり）
- **作業量**: 普通

### 3-2. アーカイブ機能
- **IMP-2**: アーカイブ機能の実装
  - 手動アーカイブ（完了・キャンセル済みのみ）
  - 自動アーカイブ（`completed_at` からN日経過）
  - 親子連動アーカイブ（親アーカイブ→子もまとめてアーカイブ）
  - アーカイブ済みタブでの閲覧・復元
- **DB変更**: `tasks` に `archived_at TEXT` カラム追加、`app_settings` に自動アーカイブ日数 INSERT
- **検証**: STEP A + STEP B + STEP R（DB変更あり）
- **作業量**: 重い

### 3-3. ソートON/OFF + 手動並び替え
- **IMP-4**: 自動ソートのON/OFF切替 + DnDによる手動並び替え
  - DnD操作体系: ギャップ→並び替え、タスク上→ネスト、UnnestGap→アンネスト（共存）
  - 「やるタスク」「タスク一覧」それぞれで独立した並び順
  - 新規タスクはリスト先頭に挿入
- **DB変更**: `tasks` に `sort_order INTEGER` カラム追加
- **検証**: STEP A + STEP B + STEP R（バージョン最終枝番）
- **作業量**: 重い

---

## v2.0.0 — テーマ・新画面・構造拡張

> 見た目の刷新と、アプリの構造を拡張する大型機能。大きな変更のため最後にまとめて実施。

### 4-1. テーマカラー変更
- **IMP-8**: ライト/ダーク + モダンな配色5パターン程度
  - `globals.css` のCSS変数をテーマごとに切り替え
  - `app_settings` にテーマ設定を保存
- **検証**: STEP A + STEP B + STEP R（UI変更あり）
- **作業量**: 重い

### 4-2. ダッシュボード改善 + 子タスク自動完了オプション
- **ENH-1**: ダッシュボードで今日完了したタスクを可視化（UI提案ベースで実装時に検討）
- **ENH-5**: 「子タスクが全完了したら親タスクも完了にする」オプション
- **DB変更**: `app_settings` に自動完了オプション INSERT
- **検証**: STEP A + STEP B + STEP R（DB変更あり）
- **作業量**: 普通

### 4-3. 「やったタスク」画面（新規）
- **ENH-4**: `completed_at` をもとに過去の完了タスクをカレンダービューで振り返る新画面
  - サイドバーナビに追加
- **検証**: STEP A + STEP B + STEP R（UI変更あり）
- **作業量**: 重い

### 4-4. プロジェクト機能
- **ENH-2**: タスク・ルーティンをプロジェクト単位でフォルダ管理
  - デフォルトは「General」プロジェクト
  - プロジェクトのアーカイブ・復元
  - 「やるタスク」画面でプロジェクト名・親タスク名を表示
- **DB変更**: `projects` テーブル新設、`tasks` / `routines` に `project_id` カラム追加（既存データはGeneralに自動割当）
- **検証**: STEP A + STEP B + STEP R（DB変更あり）
- **作業量**: 重い

### 4-5. ユーザー定義カスタムフィールド
- **ENH-3**: タスクの入力項目をユーザーが自由に追加・削除
  - テキスト・リスト・数字の3タイプ
- **DB変更**: `custom_fields` / `task_custom_values` テーブル新設
- **検証**: STEP A + STEP B + STEP C + STEP R（メジャーバージョン最終枝番）
- **作業量**: 重い

---

## 全体スケジュール概観

```
v1.0.0
    │
    │  ← v1.1.0 ✅ リリース済（2026-02-25）
    ├─ 1-1  BUG-1 + BUG-2 ✅
    ├─ 1-2  IMP-10 + IMP-11 ✅
    ├─ 1-3  IMP-9 + IMP-5 ✅
    └─ 1-4  IMP-1 + IMP-6 ✅
    │
    ▼ リリース済
v1.1.0（現在地）
    │
    │  ← v1.2.0 実装内容（2-1〜2-4 すべて完了後に検証・リリース）
    ├─ 2-1  BUG-4 + BUG-6
    ├─ 2-2  BUG-5 + IMP-12 + ENH-6
    ├─ 2-3  IMP-3
    └─ 2-4  IMP-7
    │
    ▼ 検証・リリース
v1.2.0
    │
    │  ← v1.3.0 実装内容（3-1〜3-3 すべて完了後に検証・リリース）
    ├─ 3-1  BUG-3
    ├─ 3-2  IMP-2
    └─ 3-3  IMP-4
    │
    ▼ 検証・リリース
v1.3.0
    │
    │  ← v2.0.0 実装内容（4-1〜4-5 すべて完了後に検証・リリース）
    ├─ 4-1  IMP-8
    ├─ 4-2  ENH-1 + ENH-5
    ├─ 4-3  ENH-4
    ├─ 4-4  ENH-2
    └─ 4-5  ENH-3
    │
    ▼ 検証・リリース
v2.0.0
```

**合計**: リリース4回（v1.1.0済 + 残り3回）、プロンプト16回（v1.1.0済4回 + 残り12回）
