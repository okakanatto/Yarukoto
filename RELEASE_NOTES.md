## v1.10.0（2026-03-15）

### 新機能
- ENH-9: SQLiteデータベースの手動バックアップ・復元機能を追加しました。設定画面の「データ管理」タブからバックアップファイルを任意の場所に保存したり、過去のバックアップから復元できます。復元前には自動バックアップも行われます
- ENH-8: タスク一覧画面にテキスト検索バーを追加しました。タイトル・メモ・タグ名を対象にインクリメンタル検索でき、既存のフィルタ・ソートと組み合わせて使用できます

### 改善
- IMP-26: CSVエクスポートの出力フィールドを大幅に拡充しました。タグ・プロジェクト・parent_id・completed_at・archived_at などを含む全22フィールドが出力されます。アーカイブ済みタスクも含めて出力されます
- IMP-27: CSVインポートの対応フィールドを拡充し、エクスポートしたCSVをそのままインポートして復元するラウンドトリップに対応しました。タイトルのみの既存CSVも引き続きインポートできます
- IMP-28: データベースのマイグレーション管理をバージョン番号ベースの方式に改善しました。ユーザーへの直接的な影響はありませんが、今後のアップデート適用がより確実になります
- IMP-42: タスク一覧・今日やるタスク画面でのドラッグ＆ドロップ並び替え時の視覚的フィードバックを改善しました。ドラッグ中のプレースホルダー表示、ドロップ後のアニメーション追加、ドラッグハンドルアイコンの統一などにより、並び替え操作の感触が大幅に向上しました

---

## v1.9.2（2026-03-14）

### バグ修正
- BUG-15: タスク追加時にプロジェクトの初期値が常に「Inbox」になってしまうリグレッションを修正しました。プロジェクト画面など各コンテキストに応じた初期値が正しく引き継がれるようになりました
- BUG-16: ダッシュボード「あしあとマップ」でセルを選択した際に表示されるツールチップが、画面上端付近で見切れてしまう問題を修正しました

### 改善
- IMP-43: FABボタン（右下の＋ボタン）からタスク追加モーダルを開いた際に表示されていた折りたたみボタン（−）を削除しました。×ボタンと役割が重複していたため整理しました

### 既知の問題
- CSVエクスポートが不完全（タグ・プロジェクト・parent_idなど一部データが出力されない）
- SQLiteデータベースのバックアップ・復元機能が未実装

---

## v1.9.1（2026-03-11）

### バグ修正
- BUG-13: 「今日やるタスク」画面で手動並び替えモード中にタスクを完了すると、並び順が意図せず変わってしまう問題を修正しました
- BUG-14: ダッシュボード「あしあとマップ」のヒートマップ上部にある月ラベルが見切れて表示される問題を修正しました

### 既知の問題
- CSVエクスポートが不完全（タグ・プロジェクト・parent_idなど一部データが出力されない）
- SQLiteデータベースのバックアップ・復元機能が未実装

---

## v1.9.0（2026-03-11）

### 改善
- IMP-41: ダッシュボードを全面再設計しました。先延ばしの心理学（TMT理論）に基づき、「自分は進めている」と感じられる動機づけ画面に生まれ変わりました
  - **今週のサマリー**: 今月・今週・今日の完了件数と追加件数を一覧表示。プロジェクト別の内訳、直近8週間の累積グラフ、完了ペースの文言フィードバックも表示されます
  - **あしあとマップ**: 過去90日間の活動をヒートマップで可視化。「90日中 ○○ 日、手が動いた」という主ラベルで自分の蓄積を確認できます
  - **自分のリズム**: 直近10週間の曜日別活動パターンをミニバーで表示し、進みやすい曜日の傾向を把握できます
  - **ベースキャンプ・ストリップ**: 画面下部に着手中のタスクとアクションボタンを常設表示し、次の一歩をすぐに踏み出せる導線を提供します

### 既知の問題
- CSVエクスポートが不完全（タグ・プロジェクト・parent_idなど一部データが出力されない）
- SQLiteデータベースのバックアップ・復元機能が未実装

---

## v1.8.2（2026-03-10）

### バグ修正
- BUG-10: タスク一覧でドラッグ＆ドロップによる親子関係の変更を行った際に「並び替えの保存に失敗しました」というエラーが表示される問題を修正しました（データの保存自体は正常に行われていました）
- BUG-11: タスク編集モーダルで保存を行った際に「保存に失敗しました」というエラーが表示される問題を修正しました
- BUG-12: 「今日やるタスク」画面の進捗リングの達成割合が、フィルター条件によって変動してしまう問題を修正しました。進捗リングは常にその日のタスク全体を母数として算出されるようになりました

---

## v1.8.1（2026-03-09）

### バグ修正
- BUG-9: UI上に残っていた一部の絵文字（タブ、ボタン、バッジなど）をアイコンへ置き換えました

### 改善
- IMP-35: ダークモード使用時の補助テキストの視認性を向上させました
- IMP-36: タスク追加時に、開いている画面に合わせてプロジェクトの初期値が自動で選択されるようになりました（例：プロジェクト画面ではそのプロジェクトが自動選択されます）
- IMP-37: タスク入力フォームでの入力順序（Tabキーでの移動順）を使いやすく最適化しました
- IMP-38: 「今日やるタスク」画面で、同じ親タスク内の子タスク同士をドラッグ＆ドロップで並び替えられるようになりました
- IMP-39: タスクをドラッグ＆ドロップで移動（親子関係の変更）した際に、移動先に合わせてプロジェクトが自動で同期されるようになりました

---

## v1.8.0（2026-03-08）

### 改善
- IMP-8: UI全面リニューアル + テーマシステム（ライト/ダーク + アクセント6色）
- IMP-29: エラー画面にエラー詳細の折りたたみ表示＋コピーボタンを追加
- IMP-30: 設定画面にアプリバージョン番号を表示

### 既知の問題
- CSVエクスポートが不完全（タグ・プロジェクト・parent_idなど一部データが出力されない）
- SQLiteデータベースのバックアップ・復元機能が未実装
- タスクカードの情報階層化（デザインリニューアルの完全化）が未完了

---

## v1.7.1（2026-03-08）

### 改善
- IMP-21: タスク追加フォームや編集画面で、キーボードによる操作（Tab, Enter, 矢印キーなど）で素早く入力操作が行えるように改善しました
- IMP-22: タスク追加・編集フォームの入力項目の並び順を見直し、より自然な順序になるように統一しました
- IMP-23: FABボタン（右下の＋ボタン）からタスク追加フォームを開いた直後に、タスク名入力欄へ自動でフォーカスを当てるようにしました
- IMP-24: タスク追加フォームから保存した際、フォームが閉じずに空の状態で再表示されるようにし、タスクの連続入力がより行いやすくなりました
- IMP-25: デフォルトのプロジェクト名を「General」から「Inbox」に変更しました

---

## v1.7.0（2026-03-07）### 改善
- IMP-20: アーカイブ一覧のUIを改善し、大量データでも快適に操作できるよう月別グルーピング・遅延ロード・テキスト検索機能を追加しました

---

## v1.6.1（2026-03-07）

### 改善（リファクタリング）
- IMP-16: DB操作関係の共通処理化と日付ユーティリティの集約を行いました
- IMP-17: 「今日やるタスク」画面の内部構造を整理し、保守性を向上させました
- IMP-18: 「やったタスク」画面およびタスク一覧の内部処理をスリム化しました
- IMP-19: 設定画面の各パネルの共通化と、タスク操作関連の処理の分割を行いました
※ いずれも内部的なコード整理であり、機能の変更はありません。

---

## v1.6.0（2026-03-06）

### バグ修正
- BUG-8: タスク一覧画面でタスクを完了にした際に、チェックマークのアニメーション（キラキラエフェクト）が再生されない問題を修正しました

### 新機能
- ENH-2: タスクやルーティンを「プロジェクト」単位でグルーピングできるようになりました。サイドバーからプロジェクトを選択して、プロジェクト単位でタスクを管理できます
  - デフォルトの「General」プロジェクトに加え、自由にプロジェクトを追加可能
  - タスク一覧・ルーティンのプロジェクトフィルタに対応
  - プロジェクト単位のアーカイブ・復元に対応

---

## v1.5.0（2026-03-06）

### 改善
- IMP-14: 「今日やるタスク」画面でアーカイブ済みの完了タスクも表示されるようになりました。アーカイブはタスク一覧の整理用であり、今日の振り返りには影響しません
- IMP-15: 「今日やるタスク」画面で親子タスクがタスク一覧と同様の親子構造（インデント付き）で表示されるようになりました

### 新機能
- ENH-4: 過去に完了したタスクをカレンダー形式で振り返れる「やったタスク」画面を新設しました。サイドバーからアクセスできます

---

## v1.4.0（2026-03-04）

### バグ修正
- BUG-7: 完了済みタスクをアーカイブ・復元した際にエラーが発生し、データ操作が正常に行えない問題を修正

### 改善
- IMP-13: 「今日やるタスク」画面の手動並び替え操作をタスク一覧画面と同じ方法（ギャップへのドロップ）に統一

### 新機能
- ENH-1: ダッシュボード画面で「今日完了したタスク」を見返せるように可視化を追加
- ENH-5: 子タスクがすべて完了した際に、自動的に親タスクを完了へ変更する設定オプションを追加

---

## v1.3.1（2026-03-01）

### 内部改善（リファクタリング）
- コードの可読性・保守性向上のため、肥大化していたファイルを分割・整理しました（ユーザー向けの機能変更はありません）
  - 共通ユーティリティを `lib/utils.js` / `lib/taskSorter.js` / `hooks/useFilterOptions.js` に抽出（Phase 0）
  - `TaskList.js`（1032行）を `TaskItem.js`・`hooks/useTaskActions.js`・`hooks/useTaskDnD.js`・`DndGaps.js` に分割（Phase 1）
  - `today/page.js`（859行）を `hooks/useTodayTasks.js`・`hooks/useDragReorder.js` に分割（Phase 2）
  - `settings/page.js`（893行）を4つのタブパネルコンポーネントに分割、`routines/page.js` からフォームモーダルを分離（Phase 3+4）

---

## v1.3.0（2026-03-01）

### バグ修正
- BUG-3: ステータスの並び順を設定画面で自由に変更できるよう修正

### 改善
- IMP-2: 完了・キャンセル済みタスクのアーカイブ機能を追加（手動アーカイブ、自動アーカイブ、親子連動、復元に対応）
- IMP-4: タスク一覧・今日やるタスクで自動ソートのON/OFF切替と、ドラッグ&ドロップによる手動並び替えに対応

---

## v1.2.0（2026-02-28）

### バグ修正
- BUG-4: 親タスクが削除されても子タスクが消えずに独立するよう修正
- BUG-5: タスク追加・編集画面での入力項目の並び順を統一
- BUG-6: すでに子タスクを持つタスクに対して、誤って親タスクを設定できないよう制限を追加

### 改善
- IMP-3: フィルター条件（ステータス、タグなど）の複数選択に対応
- IMP-7: 不要になったタグを一覧から隠せる「アーカイブ機能」を追加
- IMP-12: タスクのチェックボックスで、着手中から未着手へ戻す操作に対応

### 新機能
- ENH-6: タスクを完了した際に、完了日が画面リロードなしで即時表示されるよう改善
