# Work Log

## 最新の作業（2026-03-01）

- **フェーズ**: v1.4.0 動作確認バグ修正（カード表示崩れ + DB重複クリーンアップ）
- **対象バージョン**: v1.4.0
- **対象課題**:
  1. 「今日やるタスク」画面でカードが表示されず、タスク名が縦並びに崩れる不具合
  2. タスク・ルーティンのDB重複レコード
- **ステータス**: ✅ 完了
- **やったこと**:
  - **カード表示崩れの根本原因特定と修正**:
    - IMP-13（v1.4.0 リファクタリング）で `TodayCardItem` を `TodayPage` から別の関数コンポーネントとして抽出した際、styled-jsx のスコープが切れた。styled-jsx の `<style jsx>` は定義元コンポーネントの要素にのみ適用され、子コンポーネントには届かない
    - `app/today/page.js` の `<style jsx>` を `<style jsx global>` に変更（1文字の修正）
  - **DB重複レコードの調査と手動クリーンアップ**:
    - SQLite DB (`C:/Users/okdtk/AppData/Roaming/com.yarukoto.app/tasks.db`) を直接調査
    - タスク5件（id: 24-28）とルーティン3件（id: 8-10）が重複していることを確認。いずれも2026-02-28にオリジナルの約45秒後に作成されたレコード
    - Python スクリプトで関連する task_tags, routine_tags, routine_completions とともに重複レコードを削除
    - 削除後の検証: アクティブタスク8件、アクティブルーティン3件、重複なし
  - **前回までの修正（維持）**: クロスエンティティ重複排除（useTodayTasks.js, layout.js）、weekdays_only チェック追加（holidayService.js）、防御的修正（showOverdue初期値、masterDataReadyガード、Set重複排除）
- **変更したファイル**: `app/today/page.js`（styled-jsx global化）
- **DB操作**: 重複レコード削除（tasks id:24-28, routines id:8-10 + 関連テーブル）
- **影響範囲**: 「今日やるタスク」画面のカード表示。`<style jsx global>` にしたため、同一セレクタ名が他コンポーネントで使われた場合にスタイル衝突の可能性があるが、`.today-card` 等のプレフィックス付き命名規則により実質的なリスクは低い
- **次にやるべきこと**:
  - 実機でアプリを再起動し、カード表示が v1.3.1 と同様に復元されていることを確認する
  - サイドバー件数とメインページ件数が一致すること、重複タスク・ルーティンがないことを確認する
  - フェーズ5（動作確認）を続行する
- **注意事項・申し送り**:
  - DB重複の発生原因は不明（45秒差で同一データが作成されている。アプリの操作ミスか、初期化ロジックの問題か）。再発した場合は `lib/db.js` のシード処理を確認すべき
  - **修正の全経緯**: ①useTodayTasks.js 防御的修正（原因分析誤り）→ ②holidayService.js weekdays_only 修正（部分解消）→ ③クロスエンティティ重複排除 → ④**styled-jsx global 修正（カード表示崩れの根本解消）** → ⑤DB重複レコード手動クリーンアップ

---

## 過去の作業（直近2件まで保持。3件目以降は削除すること）

### 2026-03-01 — v1.4.0 動作確認バグ修正（クロスエンティティ重複排除 — 3回目修正）
- ステータス: ✅ 完了（ただしカード表示崩れは別問題として残存していた）
- やったこと: タスクとルーティン間のタイトルベース重複排除を useTodayTasks.js と layout.js に追加
- 変更したファイル: `hooks/useTodayTasks.js`, `app/layout.js`

### 2026-03-01 — v1.4.0 動作確認バグ修正（weekdays_only チェック欠落修正 — 2回目修正）
- ステータス: ⚠️ 部分解消（件数を9→7に削減したが、同名タスク+ルーティンの重複は未解消）
- やったこと: `isRoutineActiveOnDate()` の daily ルーティン判定に weekdays_only チェック追加
- 変更したファイル: `lib/holidayService.js`
